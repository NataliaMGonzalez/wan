program test;

int a, b, c;

# A character that can bend the elements.
int factorial(int n) {
  if (n < 2) {
    return 1;
  }
  print(n);
  return n * factorial(n - 1);
}

a = factorial(4);
b = a * 5;
c = 6;
