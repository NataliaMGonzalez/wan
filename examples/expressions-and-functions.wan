program test;

int a, b, c;

# A character that can bend the elements.
int factorial(int n) {
  bool is_last;
  is_last = n - 1;
  if (is_last) {
    return 2;
  }
  return n * factorial(n - 3);
}

a = factorial(4);
b = a * 5;
c = 6;
