program test;
int a, b, c;

# A character that can bend the elements.
int test(int n) {
  if (n > 20){
    n = 3 * 2;
    while(n < 10){
    n = n + 1;
    }
  }
  return 0;
}


b = a * 10;
c = 6;
test(5);